# Clone the repo
```bash
git clone https://github.com/Walid-N-bit/hexagon.git
```

# Run the program
```bash
python3 app.py
```
# Input
You can change the input in `main()`.

- F -> forward step
- R -> turn 90° clockwise in normal mode, 90° counter-clockwise in glithched mode
- L -> turn 90° counter-clockwise in normal mode, 90° clockwise in glithched mode
